# Prompts

[x] 1. Provide the exact additions needed for my pubspec.yaml to include: flutter_riverpod, cloud_firestore, firebase_core, firebase_auth, and shared_preferences.

Read @REQUIREMENTS.md and implement Step 1.1 and only step 1.1

[x] 2. Read @REQUIREMENTS.md and implement Step 2.1: Multi-Modal Calendar UI by creating a StateProvider in /providers to manage the three athlete modes (Athletic, Academic, Recovery), developing a CalendarScreen in /screens that dynamically updates its color palette and header icons based on that state, and providing a corresponding widget test to verify the UI correctly reflects the mode change. Follow the Ai assistant guardrails closely in @REQUIREMENTS.md.

[x] 3. Read @REQUIREMENTS.md and implement Step 2.2.1. Global Schedule Action Button by adding a mode-aware FloatingActionButton to the CalendarScreen that triggers a placeholder Modal Bottom Sheet and includes a widget test to verify the button color matches the active theme.
Read @REQUIREMENTS.md and implement Step 2.2.1 and only step 2.2.1. Follow the Ai assistant guardrails closely in @REQUIREMENTS.md.

[x] 4. Read @REQUIREMENTS.md and implement Step 2.2.2.1: Contextual Activity Chips by creating an ActivityInputSheet widget in /widgets that uses Riverpod to display and highlight mode-specific selectable chips (Athletic: Lift, Practice, Game, Film, Travel; Academic: Class, Lab, Study, Exam, Office Hours; Recovery: Injury Rehab, Ice Bath, Stretching, Hydration, Nap) using the active theme color, including a widget test to verify the dynamic chip filtering, while strictly stopping before adding any time pickers or submission buttons to maintain stepwise execution. Read @REQUIREMENTS.md and implement Step 2.2.2.1 and only step 2.2.2.1. Follow the Ai assistant guardrails closely in @REQUIREMENTS.md.

[x] 5. Read @REQUIREMENTS.md and implement Step 2.2.2.2: Date Context & Time Interval Selection by updating the ActivityInputSheet widget to require a DateTime selectedDate parameter passed from the CalendarScreen's FloatingActionButton, and adding 'Start Time' and 'End Time' UI buttons that trigger Flutter's native showTimePicker to store and display the chosen TimeOfDay values in local state, alongside updating the relevant widget tests, while strictly stopping before implementing the 'Log Activity' submission button or any global state saving logic. Read @REQUIREMENTS.md and implement Step 2.2.2.2 and only step 2.2.2.2. Follow the Ai assistant guardrails closely in @REQUIREMENTS.md.

[x] 6. Read @REQUIREMENTS.md and implement Step 2.2.2.3: Log Activity Submission by adding a 'Log Activity' button to the ActivityInputSheet that validates both an activity chip and time interval have been selected, captures this data alongside the selectedDate, and safely closes the modal via Navigator.pop(context, payload) to return the data, including widget tests to verify validation and submission, while strictly stopping before creating the unified ActivityModel or global Riverpod StateNotifier required in Step 2.3. Read @REQUIREMENTS.md and implement Step 2.2.2.3 and only step 2.2.2.3. Follow the Ai assistant guardrails closely in @REQUIREMENTS.md.

[x] 7.  Read @REQUIREMENTS.md and implement Step 2.2.3: Modal Theme & Interaction Testing by auditing the `ActivityInputSheet` to ensure all UI elements (chips, text, date picker, time pickers, and the submit button) dynamically inherit the correct `AppTheme` primary color based on the active `athleteModeProvider`, and expanding the widget test suite to comprehensively verify these interactions and styling changes, while strictly stopping before creating the `ActivityModel` or Riverpod `StateNotifier` required in Step 2.3. Read @REQUIREMENTS.md and implement Step 2.2.3  and only step 2.2.3. Follow the Ai assistant guardrails closely in @REQUIREMENTS.md.

[x] 8. Read @REQUIREMENTS.md and implement Step 2.2.4: Date UI Polish & Validation in `ActivityInputSheet`. First, restyle the Date display from a plain text `InkWell` into an `OutlinedButton` (matching the visual style of the `TimeSelector` widgets) so it is obvious to the user that it is an editable input. Second, update the `_selectDate` function's `showDatePicker` to use `firstDate: DateTime.now()` to strictly prevent the selection of past dates for predictive burnout integrity. Ensure the button's border and text colors inherit the active `athleteModeProvider` theme. Read @REQUIREMENTS.md and implement Step 2.2.4  and only step 2.2.4. Follow the Ai assistant guardrails closely in @REQUIREMENTS.md.

[x] 9. Read @REQUIREMENTS.md and implement Step 2.3.1: The `ActivityModel` Data Class by creating a strictly typed immutable Dart class in `lib/models/activity_model.dart` that defines the data structure for logged events, including explicitly typed properties for `id` (String), `title` (String), `date` (DateTime), `startTime` (TimeOfDay), `endTime` (TimeOfDay), and `category` (AthleteMode), along with a `copyWith` method, while strictly stopping before creating the Riverpod `StateNotifier` or modifying any calendar UI logic. Read @REQUIREMENTS.md and implement Step 2.3.1 and only step 2.3.1. Follow the Ai assistant guardrails closely in @REQUIREMENTS.md.

[x] 10. Read @REQUIREMENTS.md and implement Step 2.3.2: The Local State Vault by creating a Riverpod `Notifier` (or `StateNotifier`) and its corresponding provider in `lib/providers/activity_provider.dart` to manage a state of `List<ActivityModel>`, including an `addActivity(ActivityModel activity)` method that immutably updates the state list, while strictly stopping before modifying the `CalendarScreen` or `ActivityInputSheet` UI to actually save or display the data. Read @REQUIREMENTS.md and implement Step 2.3.2 and only step 2.3.2. Follow the Ai assistant guardrails closely in @REQUIREMENTS.md.

[x] 11. Read @REQUIREMENTS.md and implement Step 2.3.3: The Save Pipeline by updating the Floating Action Button's `onPressed` logic in `CalendarScreen` to `await` the data payload returned from `ActivityInputSheet`, explicitly map that raw payload into a strictly typed `ActivityModel` (generating a unique `id` using `DateTime.now().toString()`), and save it to the global state using `ref.read(activityProvider.notifier).addActivity()`, while strictly stopping before attempting to render the saved activities onto the calendar UI. Read @REQUIREMENTS.md and implement Step 2.3.3 and only step 2.3.3. Follow the Ai assistant guardrails closely in @REQUIREMENTS.md.

[x] 12. Read @REQUIREMENTS.md and implement Step 2.4.1: The Activity Card Widget by creating a reusable, stateless `ActivityCard` widget in `lib/widgets/activity_card.dart` that requires an `ActivityModel` parameter and visually displays its `title`, formatted `startTime` and `endTime`, and a relevant icon, ensuring the widget's border or background styling dynamically inherits the specific primary color of the model's `AthleteMode` category, while strictly stopping before modifying the `CalendarScreen` or building the list view. Read @REQUIREMENTS.md and implement Step 2.4.1 and only step 2.4.1. Follow the Ai assistant guardrails closely in @REQUIREMENTS.md.

[x] 13. Read @REQUIREMENTS.md and implement Step 2.4.2: The Overview Tab & Calendar Rendering by updating the header in `lib/screens/calendar_screen.dart` to include a fourth "Overview" tab icon, and updating the body to use `ref.watch(activityProvider)` to render a `ListView.builder` of `ActivityCard` widgets, ensuring the list is sorted chronologically and either displays all activities for the currently selected date when "Overview" is active, or filters them strictly by the selected `AthleteMode` for the other tabs. Read @REQUIREMENTS.md and implement Step 2.4.2 and only step 2.4.2. Follow the Ai assistant guardrails closely in @REQUIREMENTS.md.

[x] 14. Read @REQUIREMENTS.md and implement Step 2.5.1: Vault Modification Methods by updating the `ActivityNotifier` inside `lib/providers/activity_provider.dart` to include two new methods: `updateActivity(ActivityModel updatedActivity)` which maps over the current state to replace the model with the matching ID, and `deleteActivity(String id)` which filters the state to remove the matching ID, strictly ensuring both methods enforce Riverpod's immutability rules by assigning a brand new list to `state`, while strictly stopping before modifying the `ActivityInputSheet` or any UI widgets. Read @REQUIREMENTS.md and implement Step 2.5.1 and only step 2.5.1. Follow the Ai assistant guardrails closely in @REQUIREMENTS.md.

[x] 15. Read @REQUIREMENTS.md and implement Step 2.5.2: The Edit Sheet State by updating `lib/widgets/activity_input_sheet.dart` to accept an optional `ActivityModel? existingActivity` parameter in its constructor; if this parameter is provided, initialize the state variables (date, start time, end time, and selected chip) with its data inside `initState` and dynamically change the bottom submit button text from "Log Activity" to "Update Activity", while strictly stopping before modifying the `ActivityCard` or `CalendarScreen`. Read @REQUIREMENTS.md and implement Step 2.5.2 and only step 2.5.2. Follow the Ai assistant guardrails closely in @REQUIREMENTS.md.

[x] 16. Read @REQUIREMENTS.md and implement Step 2.5.3: Card Interaction & Context Menu by updating `lib/widgets/activity_card.dart` to be a `ConsumerWidget` and adding a trailing `PopupMenuButton` (icon: `Icons.more_vert`) with two items: "Edit" (which triggers `showModalBottomSheet` to open the `ActivityInputSheet` passing the current `ActivityModel`) and "Delete" (which executes `ref.read(activityProvider.notifier).deleteActivity(activity.id)`), while strictly stopping before modifying any other steps or files. Read @REQUIREMENTS.md and implement Step 2.5.3 and only step 2.5.3. Follow the Ai assistant guardrails closely in @REQUIREMENTS.md.

[x] 17. Read @REQUIREMENTS.md and implement Step 2.6.1: The "Today Only" Overview by updating the `ref.watch` list generation logic inside `lib/screens/calendar_screen.dart` so that when the "Overview" tab is active, the list is strictly filtered to only include activities where the year, month, and day of `activity.date` match the current `DateTime.now()`, while strictly stopping before modifying the Floating Action Button or implementing future grouping. Read @REQUIREMENTS.md and implement Step 2.6.1 and only step 2.6.1. Follow the Ai assistant guardrails closely in @REQUIREMENTS.md.

[x] 18. Read @REQUIREMENTS.md and implement Step 2.6.2: FAB Visibility Control by updating `lib/screens/calendar_screen.dart` to conditionally set the `Scaffold`'s `floatingActionButton` property to return `null` when the active tab index corresponds to the "Overview" tab, ensuring the `+` button cleanly disappears from the home dashboard while remaining fully visible on the Athletic, Academic, and Recovery tabs, strictly stopping before implementing any future grouping logic. Read @REQUIREMENTS.md and implement Step 2.6.2 and only step 2.6.2. Follow the Ai assistant guardrails closely in @REQUIREMENTS.md.

[x] 19. Read @REQUIREMENTS.md and implement Step 2.6.3: Future Grouping on Mode Tabs by updating `lib/screens/calendar_screen.dart` so that when the active tab is Athletic, Academic, or Recovery, the `ref.watch` filter allows both today's and all future activities to pass, and refactor the `ListView` rendering logic to dynamically insert visual date headers (e.g., "Today", "Tomorrow", or a formatted date string) above groups of `ActivityCard`s that share the same day, strictly stopping before moving on to Phase 3 or Firebase integration. Read @REQUIREMENTS.md and implement Step 2.6.3 and only step 2.6.3. Follow the Ai assistant guardrails closely in @REQUIREMENTS.md.

[ ] 20. Read @REQUIREMENTS.md and implement Step 3.1.1: Firebase Initialization by updating `lib/main.dart` to make the `main()` function asynchronous, calling `WidgetsFlutterBinding.ensureInitialized()`, and executing `await Firebase.initializeApp(options: DefaultFirebaseOptions.currentPlatform)` before `runApp`, making sure to import the newly generated `firebase_options.dart` and `firebase_core`, while strictly stopping before creating any user models or authentication services. Read @REQUIREMENTS.md and implement Step 3.1.1 and only step 3.1.1. Follow the Ai assistant guardrails closely in @REQUIREMENTS.md.